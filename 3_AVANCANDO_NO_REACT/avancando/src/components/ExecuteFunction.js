const ExecuteFunction = ({ myFunction }) => {
    return (
        <div>
            <button onClick={myFunction}>Clique aqui pra executar a função em App</button>
        </div>
    );
};

export default ExecuteFunction;
