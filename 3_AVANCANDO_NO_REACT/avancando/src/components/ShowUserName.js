// import { useState } from 'react';

const ShowUserName = (name) => {
    return (
        <div>
            <h2>O nome do usuário: {name.name}</h2>
        </div>
    );
};

export default ShowUserName;
